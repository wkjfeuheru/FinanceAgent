"""内聚的编排图模块。

每个图模块自持**状态 + 节点 + 边 + 投影**，不再靠函数体内惰性导入绕开循环：

- ``supervisor``：领域分类、领域任务铺开与 ``Send`` 扇出、缺参追问、多域汇合与投影；
- ``conversation``：casual chat 与 FAQ 的受限 ReAct 子图；
- ``compliance``：统一合规出口。

各图从本包的规范模块导入。
"""
